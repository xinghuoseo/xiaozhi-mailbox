# 小智 AI「亲子信箱」MCP 开发文档

> 项目目录：`/www/wwwroot/xaiozhi.xinghusoeo.com`
> 技术栈：Python 3.10+ / FastAPI / Uvicorn / SQLite / Nginx 反代（宝塔面板部署）
> 小智官方文档：https://my.feishu.cn/wiki/HiPEwZ37XiitnwktX13cEM5KnSb
> 小智官方示例仓库：https://github.com/78/mcp-calculator

---

## 一、最终采用的方案

**留言信箱（方案二）为骨架 + 企业微信自建应用为通知/回复通道。**

| 编号 | 功能 | 说明 |
|---|---|---|
| F1 | 留言（孩子→妈妈） | 孩子对小智说话 → MCP 工具落库 → **企业微信群第一时间收到通知（全员可见）** |
| F2 | 读信（妈妈→孩子） | 妈妈在**企业微信单聊里直接回复应用**（或网页留言板）→ 回调入库 → 孩子说"读留言"即听到 |
| F3 | 可扩展 | 工具注册表机制，后续新增 MCP 工具（语音信、提醒、相册等）即插即用 |

### 1.1 为什么不做"实时对话"

小智设备是**回合制语音交互，服务器无法主动唤醒设备播报**。因此：

- 孩子 → 妈妈方向：**真实时**（留言 1~3 秒内企微群收到）；
- 妈妈 → 孩子方向：**准实时**（妈妈回复秒级入库，孩子开口问即读）。

这是小智设备交互模式的上限，与 MCP 实现无关，属于产品预期管理。

### 1.2 企业微信自建应用能力边界（重要，先看再开发）

| 能力 | 支持情况 | 说明 |
|---|---|---|
| 应用发消息给成员（单聊） | ✅ | `message/send`，corpid + secret + agentid |
| 应用创建群聊并拉人 | ✅ | `appchat/create`，返回 chatid |
| 应用向群发消息（全员可见） | ✅ | `appchat/send`，群里显示为"xx应用"发的消息 |
| 调用 API 需可信 IP 白名单 | ⚠️ | 管理后台配置服务器出口 IP，否则接口报错 60020 |
| **群里成员回复**被应用接收 | ❌ | 应用创建的群聊（appchat）成员消息不推送回调 |
| **成员在应用单聊中回复** | ✅ | 配置"接收消息服务器"回调 URL 后，文字回复秒级推送到我们服务器 |

> **结论**：通知发到群里让大家都能看到（满足"拉到群里、全员可见"）；**妈妈的回复必须在和应用的单聊窗口里发文字**才能自动入信箱。产品话术引导：群通知末尾提示"回复本条通知或在机器人对话窗口回复，孩子即可听到"。
>
> 实操简化：妈妈日常在**应用单聊**里发"信"即可（和跟人聊天一样）；群用于全家都能看到孩子的留言。

---

## 二、小智 MCP 原理

### 2.1 接入模式：反向 WebSocket + JSON-RPC 2.0

```
孩子说话 → 小智设备(ESP32) → 小智云端(ASR+LLM+TTS)
                                   │ LLM 判断需要调用工具
                                   ▼
                MCP 接入点（wss://...，xiaozhi.me 控制台获取）
                                   ▲
                                   │ 我们的服务主动发起 WebSocket 连接（出站，无需开入站端口）
                                   │
                       【亲子信箱服务】(本项目，单进程)
                         ├─ MCP 接入模块：处理 JSON-RPC（initialize/tools/list/tools/call）
                         ├─ FastAPI：妈妈留言板 Web + 企业微信回调
                         ├─ 企微模块：token 管理、建群、发群通知、收回调
                         └─ SQLite：留言信箱
```

关键认知：

1. **小智云端是 MCP 客户端**，我们是 MCP 服务器；
2. 我们**主动连接**它的接入点，断线自动重连；
3. 握手顺序：`initialize` → 应答协议版本与能力 → `notifications/initialized`（通知，不回）→ `tools/list` → 返回工具清单 → `tools/call` → 执行并返回结果；
4. **工具的 description 会被大模型阅读**，直接决定何时调用，必须用中文写清触发场景；
5. 返回的 text 会被 LLM 组织成口语，由设备 TTS 朗读 —— **返回适合朗读的中文短句**；
6. 接入点在 xiaozhi.me 控制台 → 智能体配置页右下角复制。**该地址等同密钥，泄露后任何人可给你的智能体注册工具，严格保密**。

---

## 三、数据模型（SQLite，WAL 模式）

```sql
CREATE TABLE IF NOT EXISTS messages (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    sender      TEXT NOT NULL CHECK(sender IN ('child','mom')),
    content     TEXT NOT NULL,
    source      TEXT NOT NULL DEFAULT 'voice',   -- voice=小智语音 / web=网页 / wecom=企业微信
    created_at  TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    read_at     TEXT                              -- mom 的留言被孩子听完则写入时间
);
CREATE INDEX IF NOT EXISTS idx_sender_read ON messages(sender, read_at);
```

---

## 四、项目结构（V2.1，模块化目录）

```
/www/wwwroot/xaiozhi.xinghusoeo.com/
├── main.py              # 入口（保留根目录，uvicorn main:app）：lifespan + 企微回调
├── app/                 # 应用包
│   ├── config.py        # 配置（.env 读取 + update_env 运行时更新）
│   ├── db.py            # SQLite：messages/devices/users/ai_settings/daily_summaries/wecom_configs
│   ├── schema.sql       # 建表语句
│   ├── auth.py          # 账号密码登录（PBKDF2 + 内存 token，7 天有效）
│   ├── ai.py            # MiniMax 国内版：chat / 连通测试 / 每日总结生成 / 补跑
│   ├── scheduler.py     # 定时任务：每天 00:05 总结前一天，启动时补跑遗漏日期
│   ├── wecom.py         # 企业微信（数据库多配置驱动）：token缓存/建群/群通知/回调验签解密
│   ├── mcp/
│   │   ├── client.py    # 小智 MCP 接入（WebSocket + JSON-RPC 2.0，按设备实例化）
│   │   └── manager.py   # 多设备生命周期 + 连通测试 + 设备ID解析(JWT)
│   ├── tools/
│   │   ├── __init__.py  # MCP 工具注册表（F3 扩展点）
│   │   └── mailbox.py   # 留言 / 读信 / 未读数（device_id 贯穿）
│   └── api/
│       └── console.py   # 控制台 API（登录/设备/记录/AI/企微）
├── static/
│   └── index.html       # 前端单页：登录 + 设备/记录/AI设置/企微配置 四个导航
├── requirements.txt
├── .env                 # 密钥配置（权限 600，不进 git）
└── 开发文档.md
```

### 4.1 数据库表

| 表 | 用途 |
|---|---|
| `messages` | 留言对话（sender: child/mom，source: voice/web/wecom，device_id 来源设备，read_at 已读） |
| `devices` | 小智设备（name 自定义名称 / agent_id 从接入点 JWT 解析 / endpoint 接入点） |
| `users` | 控制台账号（PBKDF2 加盐哈希） |
| `ai_settings` | MiniMax 配置（api_key / model / base_url，单行 id=1） |
| `daily_summaries` | 每日总结（date 唯一，short 15 字内 / full 完整总结 / status: done/failed/empty/pending） |
| `wecom_configs` | 企微自建应用配置，支持多套（name/corp_id/secret/agent_id/回调token/aes_key/mom_user/chat_id/enabled） |

---

## 五、核心代码

### 5.1 config.py

```python
import os
from dotenv import load_dotenv

load_dotenv()

# ---- 小智 ----
MCP_ENDPOINT = os.getenv("MCP_ENDPOINT", "")            # wss://... 小智 MCP 接入点

# ---- 通用 ----
DB_PATH = os.getenv("DB_PATH", "mailbox.db")
BOARD_TOKEN = os.getenv("BOARD_TOKEN", "change-me")     # 网页留言板口令

# ---- 企业微信自建应用 ----
WECOM_CORP_ID = os.getenv("WECOM_CORP_ID", "")          # 企业ID
WECOM_SECRET = os.getenv("WECOM_SECRET", "")            # 应用 Secret
WECOM_AGENT_ID = int(os.getenv("WECOM_AGENT_ID", "0"))  # 应用 AgentId
WECOM_TOKEN = os.getenv("WECOM_TOKEN", "")              # 接收消息服务器 Token
WECOM_AES_KEY = os.getenv("WECOM_AES_KEY", "")          # 接收消息服务器 EncodingAESKey
WECOM_MOM_USER = os.getenv("WECOM_MOM_USER", "")        # 妈妈的企业微信账号(单聊用)
WECOM_CHAT_ID = os.getenv("WECOM_CHAT_ID", "")          # 家庭群 chatid（首次自动建群后回填）
```

`.env` 示例：

```ini
MCP_ENDPOINT=wss://xxxxxxxx@xiaozhi.cn/mcp/xxxx
DB_PATH=/www/wwwroot/xaiozhi.xinghusoeo.com/mailbox.db
BOARD_TOKEN=随机长字符串
WECOM_CORP_ID=ww8888xxxx
WECOM_SECRET=xxxx
WECOM_AGENT_ID=1000002
WECOM_TOKEN=xxxx
WECOM_AES_KEY=43位字符串
WECOM_MOM_USER=momaccount
WECOM_CHAT_ID=
```

### 5.2 db.py

```python
import sqlite3
import config

def get_db() -> sqlite3.Connection:
    conn = sqlite3.connect(config.DB_PATH, check_same_thread=False)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    return conn

def init_db():
    with get_db() as conn:
        conn.executescript(open("schema.sql", encoding="utf8").read())

def add_message(sender: str, content: str, source: str) -> int:
    with get_db() as conn:
        cur = conn.execute(
            "INSERT INTO messages(sender, content, source) VALUES(?,?,?)",
            (sender, content.strip(), source))
        return cur.lastrowid

def unread_mom_messages(limit: int = 5):
    with get_db() as conn:
        return conn.execute(
            "SELECT id, content, created_at FROM messages "
            "WHERE sender='mom' AND read_at IS NULL ORDER BY id DESC LIMIT ?",
            (limit,)).fetchall()

def mark_read(ids: list):
    if not ids:
        return
    with get_db() as conn:
        conn.execute(
            "UPDATE messages SET read_at=datetime('now','localtime') "
            "WHERE sender='mom' AND read_at IS NULL AND id IN (%s)" % ",".join("?" * len(ids)), ids)

def unread_mom_count() -> int:
    with get_db() as conn:
        return conn.execute(
            "SELECT COUNT(*) c FROM messages WHERE sender='mom' AND read_at IS NULL").fetchone()["c"]
```

### 5.3 mcp_client.py（MCP 接入核心）

```python
import asyncio, json, logging
import websockets

log = logging.getLogger("mcp")

class XiaozhiMCPClient:
    """反向接入：我们主动连接小智云端 MCP 接入点，按 JSON-RPC 2.0 应答"""

    def __init__(self, endpoint: str, registry: dict):
        self.endpoint = endpoint
        self.registry = registry  # {name: {"description","schema","handler"}}

    async def start(self):
        while True:
            try:
                async with websockets.connect(self.endpoint, max_size=None) as ws:
                    log.info("已连接小智 MCP 接入点")
                    async for raw in ws:
                        asyncio.create_task(self._handle(ws, json.loads(raw)))
            except Exception as e:
                log.error("MCP 连接断开: %s，5 秒后重连", e)
                await asyncio.sleep(5)

    async def _handle(self, ws, msg: dict):
        method, mid, params = msg.get("method"), msg.get("id"), msg.get("params") or {}

        if method == "initialize":
            await self._send(ws, {"jsonrpc": "2.0", "id": mid, "result": {
                "protocolVersion": params.get("protocolVersion", "2024-11-05"),
                "capabilities": {"tools": {}},
                "serverInfo": {"name": "family-mailbox", "version": "1.0.0"}}})

        elif method == "notifications/initialized":
            pass  # 通知，无需应答

        elif method == "tools/list":
            tools = [{"name": n, "description": t["description"], "inputSchema": t["schema"]}
                     for n, t in self.registry.items()]
            await self._send(ws, {"jsonrpc": "2.0", "id": mid, "result": {"tools": tools}})

        elif method == "tools/call":
            name, args = params.get("name"), params.get("arguments") or {}
            tool = self.registry.get(name)
            try:
                if not tool:
                    raise ValueError(f"未知工具: {name}")
                result = tool["handler"](**args)
            except Exception as e:
                log.exception("工具执行失败: %s", name)
                result = {"success": False, "result": "操作出了点小问题，等会儿再试吧"}
            await self._send(ws, {"jsonrpc": "2.0", "id": mid, "result": {
                "content": [{"type": "text", "text": json.dumps(result, ensure_ascii=False)}],
                "isError": False}})

    async def _send(self, ws, data: dict):
        await ws.send(json.dumps(data, ensure_ascii=False))
```

### 5.4 tools/mailbox.py（三大业务工具）

```python
import db

def send_message_to_mom(content: str) -> dict:
    """当孩子想给妈妈留言、发消息、说心里话时调用此工具，content 为要转达的原话"""
    content = (content or "").strip()
    if not content or len(content) < 2:
        return {"success": False, "result": "没有听清你要留言的内容，再说一遍好吗"}
    db.add_message("child", content, "voice")
    try:
        import wecom
        wecom.notify_new_child_message(content)  # 群通知 + 妈妈单聊，失败不影响主流程
    except Exception:
        pass
    return {"success": True, "result": "妈妈已经收到你的留言啦，她很快就能看到哦"}

def read_messages_from_mom(limit: int = 5) -> dict:
    """当孩子想听妈妈的留言、妈妈的回信、问妈妈有没有回复时调用此工具"""
    rows = db.unread_mom_messages(min(max(limit, 1), 10))
    if not rows:
        return {"success": True, "result": "妈妈暂时还没有新的留言哦"}
    texts = [f"第{i}条：{r['content']}" for i, r in enumerate(rows, 1)]
    db.mark_read([r["id"] for r in rows])
    return {"success": True, "result": f"妈妈给你留了{len(rows)}条言。" + "；".join(texts)}

def get_unread_count() -> dict:
    """当孩子想问妈妈有没有给自己留言、有没有回信时调用此工具，只返回未读数量"""
    n = db.unread_mom_count()
    if n == 0:
        return {"success": True, "result": "妈妈暂时没有新留言"}
    return {"success": True, "result": f"妈妈有{n}条新留言，你说'读留言'我就讲给你听"}
```

### 5.5 tools/__init__.py（工具注册表 —— F3 扩展点）

```python
from .mailbox import send_message_to_mom, read_messages_from_mom, get_unread_count

def _schema(properties: dict, required: list) -> dict:
    return {"type": "object", "properties": properties, "required": required}

REGISTRY = {
    "send_message_to_mom": {
        "description": send_message_to_mom.__doc__.strip(),
        "schema": _schema({"content": {"type": "string", "description": "要转达给妈妈的留言原话"}},
                          ["content"]),
        "handler": send_message_to_mom,
    },
    "read_messages_from_mom": {
        "description": read_messages_from_mom.__doc__.strip(),
        "schema": _schema({"limit": {"type": "integer", "description": "最多读几条，默认5"}}),
        "handler": read_messages_from_mom,
    },
    "get_unread_count": {
        "description": get_unread_count.__doc__.strip(),
        "schema": _schema({}),
        "handler": get_unread_count,
    },
}
# 新增工具三步：写函数 → 注册进 REGISTRY → 重启服务
```

### 5.6 wecom.py（企业微信自建应用）

```python
import time, base64, hashlib, logging, socket, struct, threading
import httpx
from Crypto.Cipher import AES
import config

log = logging.getLogger("wecom")
API = "https://qyapi.weixin.qq.com/cgi-bin"

# ---------- access_token（带锁缓存，提前 300s 过期） ----------
_token = {"value": "", "expires": 0}
_lock = threading.Lock()

def get_access_token() -> str:
    with _lock:
        if _token["value"] and time.time() < _token["expires"]:
            return _token["value"]
        r = httpx.get(f"{API}/gettoken", params={
            "corpid": config.WECOM_CORP_ID, "corpsecret": config.WECOM_SECRET}, timeout=10).json()
        if r.get("errcode") != 0:
            raise RuntimeError(f"获取 access_token 失败: {r}")
        _token.update(value=r["access_token"], expires=time.time() + r["expires_in"] - 300)
        return _token["value"]

def _post(path: str, payload: dict) -> dict:
    r = httpx.post(f"{API}/{path}", params={"access_token": get_access_token()},
                   json=payload, timeout=10).json()
    if r.get("errcode") not in (0, None):
        raise RuntimeError(f"企微接口 {path} 失败: {r}")
    return r

# ---------- 群聊 ----------
def ensure_family_chat(mom_user: str) -> str:
    """首次运行创建家庭群（owner 为妈妈），返回 chatid；已有则直接复用"""
    if config.WECOM_CHAT_ID:
        return config.WECOM_CHAT_ID
    r = _post("appchat/create", {
        "name": "亲子信箱", "owner": mom_user, "userlist": [mom_user],
        "chatid": f"family_mailbox_{int(time.time())}"})
    chatid = r["chatid"]
    log.info("家庭群创建成功 chatid=%s，请回填到 .env 的 WECOM_CHAT_ID", chatid)
    return chatid

def send_group_text(chatid: str, text: str):
    _post("appchat/send", {"chatid": chatid, "msgtype": "text", "text": {"content": text}})

def send_private_text(user: str, text: str):
    _post("message/send", {
        "touser": user, "msgtype": "text", "agentid": config.WECOM_AGENT_ID,
        "text": {"content": text}})

# ---------- 业务通知：孩子留言 ----------
def notify_new_child_message(content: str):
    text = f"mailbox!孩子刚刚留言啦：\n\n{content}\n\n（回复机器人消息即可回信给孩子）"
    try:
        send_group_text(ensure_family_chat(config.WECOM_MOM_USER), text)
    except Exception as e:
        log.warning("群通知失败: %s", e)
    try:
        send_private_text(config.WECOM_MOM_USER, text)
    except Exception as e:
        log.warning("单聊通知失败: %s", e)

# ---------- 接收消息回调：验签与加解密（官方协议精简实现） ----------
def _signature(token: str, timestamp: str, nonce: str, encrypt: str) -> str:
    return hashlib.sha1("".join(sorted([token, timestamp, nonce, encrypt])).encode()).hexdigest()

def _prp_decrypt(aes_key: bytes, encrypt_b64: str) -> str:
    plain = AES.new(aes_key, AES.MODE_CBC, aes_key[:16]).decrypt(base64.b64decode(encrypt_b64))
    pad = plain[-1]
    plain = plain[:-pad]
    msg_len = struct.unpack(">I", plain[16:20])[0]
    return plain[20:20 + msg_len].decode("utf8")   # 去掉16B随机串+4B长度，取消息体

def verify_url(msg_signature: str, timestamp: str, nonce: str, echostr: str) -> str:
    """GET 验签：解密 echostr 并原样返回给企业微信"""
    key = base64.b64decode(config.WECOM_AES_KEY + "=")
    assert _signature(config.WECOM_TOKEN, timestamp, nonce, echostr) == msg_signature, "验签失败"
    return _prp_decrypt(key, echostr)

def decrypt_message(msg_signature: str, timestamp: str, nonce: str, encrypt: str) -> str:
    """POST 回调：解密出 XML 消息"""
    key = base64.b64decode(config.WECOM_AES_KEY + "=")
    assert _signature(config.WECOM_TOKEN, timestamp, nonce, encrypt) == msg_signature, "验签失败"
    return _prp_decrypt(key, encrypt)

# ---------- 回调业务：妈妈的单聊回复入库 ----------
def on_mom_reply(content: str):
    import db
    db.add_message("mom", content, "wecom")
    log.info("妈妈回信已入信箱: %s", content)
```

依赖：`pip install pycryptodome`（AES 解密用）。

### 5.7 web/board.py（妈妈网页留言板，备用通道）

```python
from fastapi import APIRouter, Request, Form, Depends, HTTPException
from fastapi.responses import HTMLResponse
import config, db
import wecom

router = APIRouter()

def auth(request: Request):
    if request.query_params.get("token") != config.BOARD_TOKEN:
        raise HTTPException(401, "无效口令")

PAGE = """<!doctype html><meta charset=utf8>
<meta name=viewport content="width=device-width,initial-scale=1">
<title>亲子信箱</title>
<body style="font-family:sans-serif;max-width:640px;margin:16px auto;padding:0 12px">
<h3>亲子信箱 · 给孩子留言</h3>
<form method=POST action="/board/send?token=%(token)s">
<textarea name=content rows=3 style="width:100%%" placeholder="写点想对孩子说的话..." required></textarea>
<button style="margin-top:8px">投进信箱</button></form>
<h4>最近留言</h4>
%(rows)s</body>"""

@router.get("/board", response_class=HTMLResponse, dependencies=[Depends(auth)])
async def board(request: Request):
    rows = db.get_db().execute("SELECT * FROM messages ORDER BY id DESC LIMIT 50").fetchall()
    rows_html = "".join(
        f"<p>[{r['sender']}] {r['content']} <small>{r['created_at']}</small></p>" for r in rows)
    return PAGE % {"token": config.BOARD_TOKEN, "rows": rows_html}

@router.post("/board/send", dependencies=[Depends(auth)])
async def send(content: str = Form(...)):
    db.add_message("mom", content, "web")
    return {"ok": True}
```

> 安全提醒（沿用你以往习惯）：留言板地址只通过带随机 token 的链接访问，不对外公开入口；必要时 Nginx 层再加 IP 限制。

### 5.8 main.py（入口）

```python
import asyncio, logging
from contextlib import async_context_manager
from fastapi import FastAPI, Request, HTTPException
from fastapi.responses import PlainTextResponse
import config, db, wecom
from mcp_client import XiaozhiMCPClient
from tools import REGISTRY
from web.board import router as board_router

logging.basicConfig(level=logging.INFO,
                    format="%(asctime)s %(name)s %(levelname)s %(message)s")

@async_context_manager
async def lifespan(app: FastAPI):
    db.init_db()
    task = asyncio.create_task(XiaozhiMCPClient(config.MCP_ENDPOINT, REGISTRY).start())
    yield
    task.cancel()

app = FastAPI(title="亲子信箱", lifespan=lifespan)
app.include_router(board_router)

# ---------- 企业微信回调 ----------
@app.get("/wecom/callback")
async def wecom_verify(msg_signature: str, timestamp: str, nonce: str, echostr: str):
    return PlainTextResponse(wecom.verify_url(msg_signature, timestamp, nonce, echostr))

@app.post("/wecom/callback")
async def wecom_event(request: Request, msg_signature: str, timestamp: str, nonce: str):
    import xml.etree.ElementTree as ET
    from fastapi.responses import Response
    xml_in = ET.fromstring(await request.body())
    plain = wecom.decrypt_message(msg_signature, timestamp, nonce, xml_in.find("Encrypt").text)
    root = ET.fromstring(plain)
    if root.findtext("MsgType") == "text":          # 妈妈在应用单聊里回复的文字
        wecom.on_mom_reply(root.findtext("Content"))
    return Response(status_code=200)                # 企微要求回复空串/200

@app.get("/health")
async def health():
    return {"ok": True}
```

### 5.9 requirements.txt

```text
fastapi
uvicorn
websockets
python-dotenv
httpx
pycryptodome
```

---

## 六、企业微信配置步骤

1. **创建自建应用**：管理后台 → 应用管理 → 创建应用（名称如"亲子信箱"）→ 记下 `AgentId`、`Secret`，企业 `CorpID` 在"我的企业"页；
2. **设置可见范围**：把妈妈（及家庭成员）加入应用可见范围；
3. **配置可信 IP 白名单**：应用详情 → 企业可信 IP → 填服务器出口 IP（`curl ifconfig.me` 查看），否则所有 API 调用报 60020；
4. **配置接收消息服务器**（用于收妈妈的单聊回复）：
   - 应用详情 → 接收消息 → 设置 API 接收 → 随机生成 Token 和 EncodingAESKey（43 位）→ 记入 `.env`；
   - URL 填 `https://xaiozhi.xinghusoeo.com/wecom/callback`（服务先部署起来再点保存，企业微信会发 GET 验签请求）；
5. **建群**：服务首次调用 `ensure_family_chat()` 自动创建"亲子信箱"群，日志里会输出 chatid，**回填到 `.env` 的 WECOM_CHAT_ID** 后重启；
6. **产品话术**：群通知已带提示"回复机器人消息即可回信给孩子"，引导妈妈在**应用单聊**中回复文字。

## 七、部署（宝塔）

1. 宝塔 → Python 项目管理器 → 添加项目：
   - 项目目录：`/www/wwwroot/xaiozhi.xinghusoeo.com`，启动方式：命令行
   - 启动命令：`uvicorn main:app --host 127.0.0.1 --port 8300`
2. 网站（Nginx）反代 `127.0.0.1:8300`，申请 HTTPS 证书（企微回调强制 HTTPS）；
3. Nginx 站点配置中追加回调路径透传（宝塔反代默认全站转发即可；若做路径限制，保留 `/wecom/callback`、`/board`、`/health`）；
4. `.env` 权限收紧：`chmod 600 .env`；确认 `.env`、`mailbox.db` 不进任何 git 仓库。

## 八、实施顺序与验收清单

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| 1 | 装依赖、写 `.env`（先只填 MCP_ENDPOINT） | 服务启动，日志出现"已连接小智 MCP 接入点" |
| 2 | 智能体角色设定补充规则：*"凡是孩子提到妈妈、留言、回信，必须调用对应工具，不要编造留言内容"* | 语音"给妈妈留言说放学想喝酸奶" → 库里出现 child 记录 |
| 3 | 语音读信 | 网页 `/board?token=xxx` 留言 → 设备说"读一下妈妈的留言" → TTS 朗读且记录变已读 |
| 4 | 企微配置（第六章） | 群通知秒级送达；单聊回复文字 → 日志"妈妈回信已入信箱" |
| 5 | 闭环联调 | 孩子：留言 → 群收到 → 妈妈单聊回信 → 孩子说"读留言" → 听到原话 |

## 九、风险与注意事项

| 风险 | 应对 |
|---|---|
| 妈妈在群里回复收不到 | 能力边界所致（见 1.2）；话术引导到应用单聊回复；网页留言板兜底 |
| MCP 接入点泄露 | `.env` 600 权限；泄露后在控制台重新生成接入点 |
| LLM 不调用/误调用工具 | description 写清触发词 + 角色提示词约束 + content 最小长度校验 |
| access_token 并发刷新 | 已用线程锁缓存，提前 300s 续期 |
| 回调地址验签失败 | 检查 Token/EncodingAESKey 与 `.env` 一致；确认 HTTPS 证书有效 |
| SQLite 并发 | 单进程低并发足够；WAL 已开启；上量再换 MySQL |
| 内容安全 | 建议留言入库前过敏感词过滤（可后续接入云服务商文本审核 API） |

## 十、后续扩展路线（F3 落地：tools/ 目录加工具即可）

- **语音原声信**：小智端录音上传，群通知直接带音频；
- **多角色信箱**：sender 扩展为成员表（爸爸/爷爷奶奶），各自独立信箱；
- **定时提醒**：睡前故事提醒、生日自动读祝福；
- **成长相册**：孩子拍照上传，妈妈端浏览。

---

## 十一、V2 控制台版（已上线）

### 11.1 功能总览

访问 `https://xiaozhi.xinghuoseo.com/` 即进入控制台（账号密码登录）：

| 导航 | 功能 |
|---|---|
| **设备** | 多设备管理：添加/修改/删除小智 MCP 接入点；设备 ID 自动从接入点 JWT 解析（agentId）；列表实时显示连接状态（绿点/红点）；"连通测试"按钮带旋转动画，独立短连接完成 MCP 握手验证；修改接入点后自动断开旧连接重连 |
| **记录** | 左侧按天聚合列表（日期 + 当天对话总条数 + AI 15 字短总结）；点击日期，右侧以聊天气泡展示当天全部对话（孩子左侧蓝色 / 妈妈右侧橙色，带时间与来源）；总结未生成/失败时可手动点击"生成/重试" |
| **AI设置** | MiniMax 国内版配置：APIKey、模型名（默认 MiniMax-M3）、接口地址（默认 `https://api.minimaxi.com/v1/text/chatcompletion_v2`）；带连通测试按钮 |
| **企微配置** | 双模式多配置管理（V2.2）：① 自建应用（URL 回调：CorpID/Secret/AgentId/回调Token/AESKey）；② 智能机器人长连接（仅需 Bot ID + Secret，无需公网回调/白名单）；编辑时密钥掩码回显（不改则保留）；操作列：连通/编辑/删除；列表可一键复制完整回调 URL；chatid 自动建群/自动记录 |

### 11.2 登录体系

- **默认账号：`admin / xiaozhi123`，首次登录后立即在右上角"修改密码"**；
- 密码 PBKDF2-SHA256 加盐存储；登录态为内存 token（7 天有效，服务重启后需重新登录）；
- 全部 `/api/*` 接口要求 `X-Auth-Token` 请求头，未登录返回 401；
- 旧的 `/board`、`/admin` token 页面已移除，统一由登录控制台替代。

### 11.3 每日总结任务机制

```
每天 00:05（scheduler.py）
  ├─ 取前一天的全部对话（孩子留言 + 妈妈回信）
  ├─ 构造 prompt 调用 MiniMax（ai.py chat()）
  ├─ 解析返回 JSON → short（15字内）+ full（150字左右完整总结）
  ├─ 写入 daily_summaries（status=done）
  └─ 顺带补跑所有遗漏日期（服务宕机错过的不丢）
服务启动时
  └─ 立即执行一次补跑（backfill_summaries）
异常处理
  ├─ AI 未配置 → 跳过（前端显示"AI 总结未生成 [生成]"）
  ├─ 当天无对话 → status=empty，前端显示"当天没有对话"
  └─ AI 调用失败 → status=failed，前端可点击重试

记录页右侧聊天框顶部有"当天总结栏"：展示该日 full 总结，右下角"总结"按钮可随时手动生成
（当天没有对话时不显示按钮）。
```

### 11.4 API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/login` | 登录 `{username,password}` → `{token}` |
| POST | `/api/logout` | 退出登录 |
| POST | `/api/change_password` | 修改密码 `{old_password,new_password}` |
| GET/POST | `/api/devices` | 设备列表（含连接状态）/ 添加设备 `{name,endpoint}` |
| PUT/DELETE | `/api/devices/{id}` | 修改（自动重连）/ 删除 |
| POST | `/api/devices/{id}/test` | 连通测试（独立短连接握手验证） |
| GET | `/api/records/days` | 按天聚合：`[{date,count,short,full,status}]` |
| GET | `/api/records/day/{date}` | 某天对话明细 |
| POST | `/api/summary/generate` | 手动生成某天总结 `{date}` |
| GET/POST | `/api/ai/settings` | 读取（key 掩码）/ 保存 AI 配置 |
| POST | `/api/ai/test` | AI 连通测试 |
| GET/POST | `/api/wecom` | 企微配置列表（密钥掩码）/ 添加（mode: app/bot） |
| PUT/DELETE | `/api/wecom/{id}` | 编辑（掩码=不修改自动保留）/ 删除 |
| POST | `/api/wecom/{id}/test` | 连通测试：app 走 gettoken；bot 连 wss 网关鉴权 |
| GET | `/wecom/callback/{config_id}` | 企微回调验签（仅 app 模式，URL 带配置 ID） |
| POST | `/wecom/callback/{config_id}` | app 模式回调：指令（查信息/@帮助）或回信入信箱 |
| GET | `/health` | 健康检查（含设备连接数） |

### 11.5 企业微信配置（V2.2 双模式）

**方式一：自建应用（URL 回调）**

1. 企微管理后台 → 应用管理 → 创建自建应用，记下 `CorpID`、`Secret`、`AgentId`；
2. 控制台 → 企微配置 → 添加配置 → 接入方式选"自建应用"，填入参数 + 接收人账号；
3. 点"连通"验证（报 60020 = 服务器 IP 未加白名单，出口 IP 见测试报错信息）；
4. 回调 URL 从列表**一键复制**（注意：企微后台填 `https://域名/wecom/callback/{ID}`，不要重复拼接路径）；
5. 妈妈回信：在**应用单聊窗口**直接发文字（群内回复无法接收）。

**方式二：智能机器人（长连接，推荐，零公网配置）**

1. 企微管理后台 → 安全与管理 → 管理工具 → 智能机器人 → 创建机器人 → API 模式创建 → 连接方式选**"使用长连接"**；
2. 页面自动生成 `Bot ID` 和 `Secret`，填入控制台弹窗即可（无需回调地址、无需白名单、无需域名备案）；
3. 点"连通"验证（真实到达企微网关 `wss://openws.work.weixin.qq.com` 鉴权）；
4. 把机器人加进家庭群，群里 @它 发条消息，系统自动记录群会话 ID；
5. 妈妈回信：群里 @机器人 发文字，或在机器人单聊里发文字，都自动入信箱。

**企微端小指令（两种模式都支持，不开放给小智设备）**

| 指令 | 效果 |
|---|---|
| `查信息 9.15` | 返回 9 月 15 日的当日对话完整总结（不写年份默认当年） |
| `查信息 2026.9.15` | 返回指定年份日期的总结 |
| `@帮助` | 返回给普通人看的使用说明 |

其他任何文字 = 妈妈的回信，自动入信箱，孩子对小智说"读留言"即可听到。

### 11.6 MiniMax 配置说明

1. 到 MiniMax 开放平台（国内版 https://platform.minimaxi.com ）创建 APIKey；
2. 控制台 → AI 设置 → 填入 APIKey → 保存 → 点"连通测试"，提示"联通成功，AI 回复：正常"即接入成功；
3. 模型名默认 `MiniMax-M3`，接口默认 `https://api.minimaxi.com/v1/text/chatcompletion_v2`；如 MiniMax 后续更换模型名/接口，可直接在页面修改；
4. 未配置 AI 时：每日总结不执行，其余功能（留言/读信/设备管理）完全不受影响。

### 11.7 部署要点（V2.1）

- 宝塔 Python 项目管理器启动命令不变：`venv/bin/uvicorn main:app --host 127.0.0.1 --port 8300`（main.py 必须在项目根目录）；
- Nginx 反代已配置（`proxy_pass http://127.0.0.1:8300`），前端为单页应用无需额外静态配置；
- 企微配置已迁移到数据库（`wecom_configs` 表），`.env` 中 `WECOM_*`、`BOARD_TOKEN` 均已废弃；`MCP_ENDPOINT` 仍保留：devices 表为空时自动导入为第一台设备，之后以设备管理为准；
- 修改代码后需在宝塔重启项目生效。


---

## 十二、V2.4 更新（已上线）

### 12.1 MCP 工具规范优化（按小智官方文档）

依据 xiaozhi.dev 官方《MCP 协议用法说明》最佳实践优化：

| 项 | 调整 |
|---|---|
| 工具命名 | 按官方建议改为层级风格：`mailbox.send_message_to_mom` / `mailbox.read_messages_from_mom` / `mailbox.get_unread_count` |
| description | 每个工具写明触发场景与孩子可能说的话（如"给妈妈留言""读留言""查留言""查信箱""妈妈有什么吗"），并说明调用效果，便于大模型准确匹配 |
| 参数 schema | content 描述要求"传入孩子原话、尽量完整"；limit 说明默认值与用途 |
| 异步工具 | `mcp_client` 支持 async handler —— `send_message_to_mom` 现为异步：先入库，再**同步等待企微推送结果** |

> 需同步在 xiaozhi.me 智能体角色设定里写明：*提到妈妈、留言、回信必须调用工具，不要编造留言内容*。

### 12.2 已读机制（重要）

| 方向 | 何时标记 | 效果 |
|---|---|---|
| 妈妈留言（mom） | 孩子通过小智 `read_messages_from_mom` 读取后，立即 `mark_read` | **已读过的不会再次出现**，每次只读未读的新留言；没有新留言时回复"妈妈暂时还没有新的留言哦" |
| 孩子留言（child） | 推送企微群**成功后**，标记"已送达" | 控制台记录页孩子气泡显示"已送达"；推送失败时不标记，孩子会听到"妈妈那边暂时没通知到"的提示 |

前端记录页气泡：妈妈消息显示"已读"（孩子听过），孩子消息显示"已送达"（已推进群）。

### 12.3 控制台直接发送

记录页右侧聊天区**下方新增发送框**：以妈妈身份直接输入留言 → 入信箱，孩子对小智说"读留言"即可听到。接口：`POST /api/records/send {content}`；day 明细接口新增 `read` 字段。

### 12.4 企微专用指令（不对小智设备开放，V2.5 修订识别规则）

| 指令（须单独发一条） | 效果 |
|---|---|
| `查未读`（或 `未读` / `未读留言` / `孩子读了吗`） | 返回**孩子还没听过**的留言数量 + 每条内容与时间列表（按时间正序） |
| `查信息 9.15` / `查信息 2026.9.15` | 返回指定日期的完整总结（不写年份默认当年） |
| `帮助` / `@帮助` | 返回使用说明 |

**V2.5 识别规则（重要）**：

1. **指令必须单独发一条消息**。指令词后面跟了任何其他字（如"查未读 谢谢""帮助我一下"），整条消息一律当作给孩子的留言；
2. `查信息` 是唯一带参数的指令，要求整条消息严格为"查信息 + 日期"结尾，日期缺失/错误/跟了别的话都按留言处理；
3. @ 前缀写法兼容三种：`@机器人 指令`（@+名字+空格）、`@指令`（手打@直接跟指令）、`@ 指令`（@+空格+指令），带不带 @ 都能识别；
4. 未读留言朗读/列表**按时间正序**：最早的先读，最新的排在最后。

该能力只在企微端（机器人消息处理）生效，未注册进 MCP 工具表，小智设备端无法调用。

### 12.5 工具描述规范参考（自定义新工具时遵守）

```
name:        建议层级命名 "模块.功能"（官方最佳实践）
description: 中文写明 ① 什么场景调用 ② 孩子可能说的话 ③ 调用后会发生什么
inputSchema: JSON Schema，properties 里每个参数写 description；支持 string/int/bool
返回值:      {"success": bool, "result": "适合 TTS 朗读的中文短句"}
```


---

## 十三、V2.5 更新（已上线）

### 13.1 留言朗读顺序调整

`read_messages_from_mom` 未读留言查询由 `id DESC` 改为 **`id ASC`（时间正序）**：孩子听留言时最早写的先读、最新的排在最后，符合"最新留言在最下面"的阅读直觉。"查未读"指令的列表顺序同步调整。

### 13.2 @ 前缀清洗重写（strip_at_prefix）

旧正则 `^@\S+\s*` 会把用户手打的 `@查未读`（@ 后无空格）整条清空，导致消息被误丢弃或误入库。重写后的清洗逻辑：

| 原始消息 | 清洗后 |
|---|---|
| `@机器人 查未读` | `查未读`（@ 后带名字+空格 → 去掉名字） |
| `@查未读` | `查未读`（手打 @ 直接跟内容 → 只去掉 @） |
| `@ 查未读` | `查未读` |
| `普通消息` | 原样 |

### 13.3 指令识别改为严格精确匹配

按用户规则："一般发查未读/帮助/查信息之后不会再跟其他字。如果跟了，就算留言。仅识别特定关键字，带 @ 和不带都一样。"

- `帮助`、`查未读`（及别名）：**整条消息精确等于指令词**才触发；
- `查信息`：整条消息严格为 `查信息 + 日期`（`9.15` / `2026.9.15`，支持 `. 。 、` 分隔）结尾才触发；
- 指令后跟任何其他字（如 `查未读 谢谢`、`帮助我一下`、`查信息 明天`）：**一律按留言入库**；
- 原"查信息格式错误提示"分支已删除（格式不对 = 留言）。

回归测试覆盖 16 个场景（带 @/不带 @ × 指令/指令带尾巴/普通消息），全部符合预期。

---

## 十四、V2.6 前端交互调整（记录页）

1. **发送块移至最下方**：右侧区域自上而下为「当天总结栏 → 聊天气泡区 → 留言发送框」，发送框固定在底部；
2. **总结栏去掉按钮**：右侧总结块不再有"总结"按钮，纯展示当天完整总结（full），**高度随内容自适应**、无竖向滚动条；
3. **总结入口移到左侧列表**：每个日期第一行右侧（与条数对齐）新增"总结"两字入口：
   - 已总结过的日期可**点击再次总结**（重新生成会覆盖旧总结）；
   - 生成失败可点击重试；未生成可点击生成；
   - "当天没有对话"的日期不显示入口；
   - 点击"总结"不会触发打开当天对话（事件已隔离）。

### V2.6 补充：高度布局

记录页右侧「总结栏 / 聊天区 / 发送框」三块合计占满视口（`.rec-wrap` 高度 = `100vh - 98px`，已计入顶栏与边距）：

- **总结栏**：高度随内容自适应，上限为右侧高度的 50%，超出部分内部滚动；
- **聊天区**：占据剩余全部高度（`flex:1`），气泡在内部滚动；
- **发送框**：固定高度（`flex:0 0 auto`）。

左侧日期列表同样随容器高度内部滚动。

---

## 十五、V2.7 运维规则与稳定性修复（2026-09-22）

### ⚠️ 运维铁律：仅允许宝塔重启服务

**服务只允许通过宝塔面板（Python 项目管理器）启动/重启/停止。**
开发调试过程中产生的任何临时进程，验证完毕后必须立即 `kill` 释放 8300 端口。

原因（实际踩坑两次）：
1. 临时进程占用 8300 → 宝塔启动报 `address already in use`，服务根本起不来；
2. 临时进程与宝塔进程**同时连接同一企微机器人** → 企微规则"同一 Bot 仅允许一个长连接"，
   两个连接会**无限互相踢线**，表现为机器人时连时断、指令全部失效。

### 🐛 修复：机器人断线后不再自动重连（严重）

**现象**：机器人长连接被踢/断开后，永远离线，所有企微指令失效。

**根因**：企微服务端踢线时通过 `disconnected_event` 事件通知，SDK 内部静默关闭连接
**不会抛出异常**，导致旧代码 `await asyncio.Event().wait()` 永久挂起，外层重连循环永远不触发。

**修复**（app/wecom_bot.py）：
- 监听 SDK 的 `disconnected` / `error` 事件 → 触发 `asyncio.Event` 唤醒外层循环 → 5 秒后自动重连；
- 已实测：连通测试抢线 → 主连接收到断线事件 → 5 秒后自动重建 ✅。

**注意**：控制台点"连通测试"会用同一 Bot 建立短连接，会**短暂踢掉主连接**（约 5~10 秒后自动恢复），
属正常现象。

### 🐛 修复：企微列表"连接状态"恒为未连接（显示 bug）

`_bot_connected()` 误读了已废弃的 `_state["ws"]` 键，改为读取 `client.connected`。

---

## 十六、V2.8 更新（2026-09-23）

### 16.1 站点访问修复（IPv6）

域名新增了 AAAA 记录（IPv6），但站点 nginx 配置未监听 IPv6，导致 IPv6 访问被主站接住（表现为跳转其他站点/404）。
另：宝塔中站点改名/重建后**反代规则会丢失**，需手动补回 `location / → proxy_pass 127.0.0.1:8300`。
已修复：`python_xiaozhi.conf` 补充反代 + `listen [::]:80/[::]:443` IPv6 监听，双栈验证 200。

### 16.2 企微推送格式调整

孩子留言推送到群/单聊的格式改为（去掉原尾部提示语）：

```
[09-23 08:51] 小智设备 留言：
我现在困了，我要睡觉了，晚安。
```

`[时间]` 为推送时刻，`设备名` 取自设备配置（多设备可区分来源）。

### 16.3 成员名单 / 申请审批（企微端准入）

导航「企微配置」更名为**「接收配置」**，页面改为左右布局：左侧二级菜单（接收配置 / 申请列表）。

**成员规则**（仅企微端，小智设备端不受影响）：

| 成员状态 | 收到其消息时的处理 |
|---|---|
| 无记录（新成员首次 @） | 自动创建申请（待通过），并回复"已提交申请，等待管理员通过"；若该配置还没有任何已通过成员，**首个发消息者自动通过**（即管理员本人） |
| 待通过（pending） | 只回复等待提示，不执行指令、不入信箱 |
| 已通过（approved） | 正常处理：指令（查信息/查未读/帮助）或回信入信箱 |
| 已拒绝（denied） | 静默忽略 |

**控制台「申请列表」**：用户ID / 所属配置 / 状态 / 时间 / 操作（通过、拒绝、删除），支持随时移除已通过成员。

**自动通过开关**：接收配置弹窗与列表页均有开关（写入 `wecom_configs.auto_approve`）——开启后新成员免审批直接可用（即"群内所有用户全通过"的效果）。

> 注意：企微机器人有**可见范围**限制，可见范围外的成员 @ 机器人消息不会推送。若要让群内所有成员可用，需在企微后台把机器人可见范围扩大，并开启"自动通过"。

### 16.4 API 新增

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/wecom/members?config_id=` | 成员/申请列表 |
| POST | `/api/wecom/members/{id}/approve` `/deny` `/delete` | 审批操作 |
| PUT | `/api/wecom/{id}` | 支持 `auto_approve` 字段 |
